// Promo Blast — the current/canonical URL for what used to be "AD DROP" (src/app/ad-drop/page.tsx
// still serves the same page at its old URL, so existing links/bookmarks keep working).
export { default } from '../ad-drop/page';
