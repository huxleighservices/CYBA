---
description: Start the Next.js preview server to test the app locally
---

### Project Preview Workflow

This workflow starts the Next.js development server.

1. Start the next server:
```bash
npm run dev
```

2. Once the server says "Ready", grab the localhost URL (e.g. `http://localhost:9002`) and run a browser subagent or manually navigate to test the app visually.
