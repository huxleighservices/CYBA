rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read;
      // Allow writes for authenticated users, up to 25MB.
      allow write: if request.auth != null && request.resource.size < 25 * 1024 * 1024;
    }
  }
}
